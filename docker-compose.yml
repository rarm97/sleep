services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: sleep
      POSTGRES_PASSWORD: sleep
      POSTGRES_DB: sleep
    ports: 
      - "5432:5432"
    volumes: 
      - dbdata:/var/lib/postgresql/data
volumes: 
  dbdata:
